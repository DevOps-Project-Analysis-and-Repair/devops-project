{
  "name": "upload-service",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/index.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outfile=dist/index.js",
    "watch": "esbuild src/index.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outfile=.aws-sam/build/LlmServiceFunction/index.js --watch",
    "dev": "sam build && sam local start-api --port 4000"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.160",
    "@types/node": "^25.3.0",
    "esbuild": "^0.27.3"
  },
  "dependencies": {
    "@aws-lambda-powertools/event-handler": "^2.31.0",
    "@aws-lambda-powertools/logger": "^2.31.0",
    "@aws-sdk/client-dynamodb": "^3.996.0",
    "@aws-sdk/client-s3": "^3.996.0",
    "openai": "^6.25.0"
  }
}
