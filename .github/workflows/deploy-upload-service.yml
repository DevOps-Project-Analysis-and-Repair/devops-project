name: Deploy UploadService

on:
  push:
    # branches:
      # - main
    paths:
      - '.github/workflows/deploy-upload-service.yml'
      - 'apps/upload-service/**'

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    env:
      AWS_REGION: eu-west-1
      GIT_HASH: ${{ github.sha }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up AWS CLI
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

# TODO: Run tests

      - name: Publish Dockerfile
        working-directory: ./apps/upload-service
        run: sh ./publish.sh

      - name: Validate CloudFormation template
        run: |
          aws cloudformation validate-template \
            --template-body file://./apps/upload-service/upload-service-stack.yml

      - name: Deploy CloudFormation
        working-directory: ./apps/upload-service
        run: sh ./deploy.sh

      # - name: Install dependencies
      #   run: npm ci
      #   working-directory: ./apps/frontend

      # - name: Build frontend
      #   run: npm run build
      #   working-directory: ./apps/frontend

      # - name: Validate CloudFormation template
      #   run: |
      #     aws cloudformation validate-template \
      #       --template-body file://./apps/upload-service/upload-service-stack.yml

      # - name: Deploy CloudFormation stack
      #   run: |
      #     aws cloudformation deploy \
      #       --template-file ./apps/upload-service/upload-service-stack.yml \
      #       --stack-name upload-service-stack \
      #       --capabilities CAPABILITY_IAM